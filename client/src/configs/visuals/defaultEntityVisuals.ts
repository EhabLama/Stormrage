import { IEntityVisualConfig } from './IEntityVisualConfig';

// Placeholder for default entity visuals
export const defaultEntityVisuals: IEntityVisualConfig = {
  textureKey: 'placeholder_entity', // A default placeholder texture
  scale: 1,
  // animations: { idle: 'default_idle_anim' } // Example
};
