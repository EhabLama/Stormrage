// Placeholder for Ability Configuration Interface
// This will define the structure for individual abilities.
// Example properties might include: id, name, description, type (e.g., 'passive', 'active', 'projectile'),
// cooldown, manaCost, damage, range, duration, effects (e.g., 'stun', 'heal'),
// animationKey, soundEffectKey, projectileConfigKey (if it launches a projectile), etc.

export interface IAbilityConfig {
  id: string;
  name: string;
  description: string;
  // ... more properties to be defined
}
