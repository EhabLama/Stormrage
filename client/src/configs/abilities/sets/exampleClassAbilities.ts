import { IAbilityConfig } from '../IAbilityConfig';
// import { exampleAbility } from '../definitions/exampleAbility'; // Example import

// Placeholder for an example class ability set
// This would define the collection of abilities available to a specific class.
export const exampleClassAbilitySet: { abilities: IAbilityConfig[] } = { // Or could be string IDs
  abilities: [
    // exampleAbility,
    // ... other abilities
  ],
};

// Add more class ability set definitions here or in separate files.
